# RAG-03 文档解析与数据治理：关系图

> 本模块关注“进入检索系统之前”的信息质量。解析错误会沿 Chunk、Embedding、检索和生成继续传播，后面很难补救。

## 图 1：来源类型决定解析路线

```mermaid
flowchart TD
    S["03-001 PDF、Word、HTML、Markdown 等来源"] -->|识别格式与能力| R{"内容形态路由"}
    R -->|有文本层| P["03-002 文本型 PDF 解析"]
    R -->|扫描或图片| O["03-003 OCR"]
    R -->|多栏或复杂版式| L["03-004 版面与阅读顺序"]
    P -->|元素及坐标| E["统一 DocumentElement"]
    O -->|文字、置信度与坐标| E
    L -->|区域与阅读顺序| E
    E -->|跨模态重新关联| M["03-015 多模态元素关联"]
```

## 图 2：特殊元素不能一律压成纯文本

```mermaid
flowchart TD
    E["统一文档元素"] -->|重复区域识别| N["03-005 页眉、页脚、脚注、目录噪声"]
    E -->|保持行列和表头| T["03-006 表格结构"]
    E -->|保留图像、Caption 与位置| I["03-007 图片、图表、流程图"]
    E -->|保留语法和边界| F["03-008 公式、代码、特殊内容"]
    T -->|与正文、Caption 和页码关联| M["03-015 多模态阅读顺序"]
    I -->|关联| M
    F -->|关联| M
    N -->|去噪但保留必要语义| M
```

## 图 3：治理、质量门禁与回退

```mermaid
flowchart TD
    P["解析结果"] -->|编码、空白和格式统一| C["03-009 清洗与标准化"]
    C -->|文档/段落/近重复检测| D["03-010 去重"]
    D -->|补充来源、时间、层级| M["03-011 元数据设计"]
    M -->|ACL、PII 与合规门禁| A["03-012 入库控制"]
    A -->|候选 Pipeline| S["03-013 工具与方案选型"]
    S -->|抽样、错误率与业务测试| Q["03-014 质量抽检"]
    Q -->|合格| O["输出给 RAG-04 Chunking"]
    Q -->|局部失败| F["回退解析器、人工复核或隔离"]
    F -->|修复后重验| Q
```

## 原子知识覆盖

| 原子 ID | 主要关系 |
|---|---|
| RAG-03-001 | `决定` 解析器和能力路由 |
| RAG-03-002 | `处理` 有文本层 PDF 并输出元素 |
| RAG-03-003 | `补偿` 扫描件无文本层问题 |
| RAG-03-004 | `恢复` 区域、层级和阅读顺序 |
| RAG-03-005 | `去除或标注` 重复版面噪声 |
| RAG-03-006 | `保留` 表格结构并支持专项检索 |
| RAG-03-007 | `连接` 视觉元素、Caption 与正文 |
| RAG-03-008 | `保护` 公式、代码和特殊语法边界 |
| RAG-03-009 | `规范化` 所有下游输入 |
| RAG-03-010 | `减少` 重复索引与答案偏置 |
| RAG-03-011 | `支撑` 过滤、追溯、时效和引用 |
| RAG-03-012 | `约束` 入库范围与在线可见性 |
| RAG-03-013 | `组合` 解析能力和回退路径 |
| RAG-03-014 | `评估并门禁` 解析结果 |
| RAG-03-015 | `重建` 跨元素关联与完整语义 |

覆盖：**15 / 15**。详细解释见[标准学习正文](../../../knowledge/rag/chapters/rag-03-document-parsing-governance.md)。

