# RAG-02 系统架构与生命周期：关系图

> 本模块把 RAG 看成一个可追踪、可替换、可恢复的系统，而不是一次 `retrieve()` 加一次模型调用。

## 图 1：离线与在线双链路

```mermaid
flowchart TD
    S["知识源"] -->|触发 02-008 批/流/事件摄取| O1["02-001 离线建库"]
    O1 -->|解析| P["02-004 解析器"]
    P -->|规范文档| C["02-004 切分器"]
    C -->|Chunk| E["02-004 嵌入器"]
    E -->|向量与元数据| I["02-004 索引"]
    I -->|发布可查询版本| O2["02-002 在线问答"]

    Q["问题"] -->|查询理解| O2
    O2 -->|召回候选| RT["02-005 Retriever"]
    RT -->|候选集| RR["02-005 Reranker"]
    RR -->|有序证据| G["02-005 Generator"]
    G -->|答案与引用| A["响应"]
```

## 图 2：三段式链路与数据契约

```mermaid
flowchart TD
    Q["标准化查询 QueryRequest"] -->|召回| C["02-003 Candidate 集合"]
    C -->|过滤、融合、重排| E["02-003 Evidence 集合"]
    E -->|上下文组装与生成| A["02-003 Answer"]

    T["02-007 来源→文档→Chunk→答案追踪"] -->|为每个对象提供 ID 与版本| C
    T -->|生成引用映射| A
    M["02-009 模块化接口"] -->|约束输入输出 Schema| Q
    M -->|允许替换 Retriever/Reranker/Generator| E
```

召回阶段追求候选覆盖，过滤/重排阶段追求相关性、权限和证据质量，生成阶段追求忠实回答。三个阶段目标不同，不能只用一个“答案好不好”指标替代分层观测。

## 图 3：数据流、控制流与错误流

```mermaid
flowchart TD
    C["02-006 控制流：任务编排与策略"] -->|发起并设定超时| D["02-006 数据流：文档、Chunk、证据、答案"]
    D -->|异常、低置信或超时| E["02-006 错误流"]
    E -->|记录状态与重试边界| S["02-010 任务状态、幂等、恢复"]
    S -->|重放未完成步骤| C

    T["02-007 Trace ID 与版本链"] -->|关联每一步输入输出| D
    T -->|定位首次故障阶段| E
    U["02-008 更新事件"] -->|创建幂等任务| S
    X["RAG-11 生产治理"] -->|超时、重试、灰度与回滚| S
```

## 原子知识覆盖

| 原子 ID | 在图中的角色 | 主要关系 |
|---|---|---|
| RAG-02-001 | 离线建库 | `产生` 可发布索引 |
| RAG-02-002 | 在线问答 | `消费` 索引并产生答案 |
| RAG-02-003 | 召回、过滤、生成 | `分解` 在线目标 |
| RAG-02-004 | 解析、切分、嵌入、索引、检索边界 | `实现` 离线和在线组件 |
| RAG-02-005 | Retriever、Reranker、Generator | `串联` 候选、证据和答案 |
| RAG-02-006 | 数据流、控制流、错误流 | `描述` 三类系统行为 |
| RAG-02-007 | 可追溯链路 | `关联` 全链路对象和版本 |
| RAG-02-008 | 批、流、事件更新 | `触发` 建库或增量任务 |
| RAG-02-009 | 模块化接口 | `约束` 数据契约并支持替换 |
| RAG-02-010 | 状态、幂等、失败恢复 | `控制` 重试和恢复 |

覆盖：**10 / 10**。详细解释见[标准学习正文](../../../knowledge/rag/chapters/rag-02-architecture-lifecycle.md)。

