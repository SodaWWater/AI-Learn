# 专栏符号对照（钉死学习树）

对照树：`ccb-src/` @ `77a7934e15d69da13879112ed7db695c9ee7a52a`

专栏：`原文/03-源码解析/`

四层名字对得上。签名和体积已经和专栏摘录不完全一样——这棵树是复刻/演进后的代码，不是专栏当时那份摘录的逐字副本。

## 主循环（01 架构 / 02 Query）

| 专栏说法 | 学习树 |
|---|---|
| `ask` | `src/QueryEngine.ts` `export async function* ask`（约 L1256）。注释写明是 QueryEngine 的一次性封装 |
| `QueryEngine.submitMessage` | `src/QueryEngine.ts` `export class QueryEngine`（L192），`async *submitMessage`（L217） |
| `query` | `src/query.ts` `export async function* query`（L276），内部 `yield* queryLoop(...)` |
| `queryLoop` | `src/query.ts` `async function* queryLoop`（L393） |

专栏摘录的 `queryLoop(params, consumedCommandUuids)` 只有两个参数。学习树多了第三参 `consumedAutonomyCommands`。`query.ts` 全文 2057 行；`queryLoop` 从 L393 起。专栏说「近 1500 行」是对当时摘录的约数。

## 其他源码篇落点

| 专栏 | 学习树优先看 |
|---|---|
| 03 Compact | `src/services/compact/`（`compact.ts`、`autoCompact.ts`、`microCompact.ts` 等）；`/compact` 命令在 `src/commands/compact/compact.ts` |
| 04 grep | `packages/builtin-tools/src/tools/GrepTool/` |
| 05 记忆 | `src/memdir/`；另有 `src/services/SessionMemory/` |
| 06 SubAgent | `packages/builtin-tools/src/tools/AgentTool/` |
| 07 Skill | `packages/builtin-tools/src/tools/SkillTool/`、`DiscoverSkillsTool/` |

内置工具目录共 64 项（含 shared/testing）。其中 Goal、Ultracode、Langfuse 等是这棵复刻树后来加的，专栏源码篇没有讲，讲解时不要当成专栏原意。
